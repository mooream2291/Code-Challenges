'use strict';

//two pointers, sorted array

function countUniqueValues(arr) {
  if(arr[i] !== arr[j]) {
    i++;
    arr[i] = arr[j];
  }
}

//sliding window

//looking for a continuos subset of data
//can be a var, subarray, or string

function countUniqueVals(arr) {
  

}